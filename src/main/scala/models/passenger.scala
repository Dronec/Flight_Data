package models

case class passenger(
                      passengerId: Int,
                      firstName: String,
                      lastName: String
                    ) extends genericModel

